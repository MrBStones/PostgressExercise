SELECT 'Projects: ID --> PID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.ID FROM Projects R GROUP BY R.ID HAVING COUNT(DISTINCT R.PID) > 1) X;
SELECT 'Projects: ID --> SID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.ID FROM Projects R GROUP BY R.ID HAVING COUNT(DISTINCT R.SID) > 1) X;
SELECT 'Projects: ID --> SN' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.ID FROM Projects R GROUP BY R.ID HAVING COUNT(DISTINCT R.SN) > 1) X;
SELECT 'Projects: ID --> PN' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.ID FROM Projects R GROUP BY R.ID HAVING COUNT(DISTINCT R.PN) > 1) X;
SELECT 'Projects: ID --> MID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.ID FROM Projects R GROUP BY R.ID HAVING COUNT(DISTINCT R.MID) > 1) X;
SELECT 'Projects: ID --> MN' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.ID FROM Projects R GROUP BY R.ID HAVING COUNT(DISTINCT R.MN) > 1) X;
SELECT 'Projects: PID --> ID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.PID FROM Projects R GROUP BY R.PID HAVING COUNT(DISTINCT R.ID) > 1) X;
SELECT 'Projects: PID --> SID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.PID FROM Projects R GROUP BY R.PID HAVING COUNT(DISTINCT R.SID) > 1) X;
SELECT 'Projects: PID --> SN' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.PID FROM Projects R GROUP BY R.PID HAVING COUNT(DISTINCT R.SN) > 1) X;
SELECT 'Projects: PID --> PN' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.PID FROM Projects R GROUP BY R.PID HAVING COUNT(DISTINCT R.PN) > 1) X;
SELECT 'Projects: PID --> MID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.PID FROM Projects R GROUP BY R.PID HAVING COUNT(DISTINCT R.MID) > 1) X;
SELECT 'Projects: PID --> MN' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.PID FROM Projects R GROUP BY R.PID HAVING COUNT(DISTINCT R.MN) > 1) X;
SELECT 'Projects: SID --> ID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.SID FROM Projects R GROUP BY R.SID HAVING COUNT(DISTINCT R.ID) > 1) X;
SELECT 'Projects: SID --> PID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.SID FROM Projects R GROUP BY R.SID HAVING COUNT(DISTINCT R.PID) > 1) X;
SELECT 'Projects: SID --> SN' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.SID FROM Projects R GROUP BY R.SID HAVING COUNT(DISTINCT R.SN) > 1) X;
SELECT 'Projects: SID --> PN' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.SID FROM Projects R GROUP BY R.SID HAVING COUNT(DISTINCT R.PN) > 1) X;
SELECT 'Projects: SID --> MID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.SID FROM Projects R GROUP BY R.SID HAVING COUNT(DISTINCT R.MID) > 1) X;
SELECT 'Projects: SID --> MN' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.SID FROM Projects R GROUP BY R.SID HAVING COUNT(DISTINCT R.MN) > 1) X;
SELECT 'Projects: SN --> ID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.SN FROM Projects R GROUP BY R.SN HAVING COUNT(DISTINCT R.ID) > 1) X;
SELECT 'Projects: SN --> PID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.SN FROM Projects R GROUP BY R.SN HAVING COUNT(DISTINCT R.PID) > 1) X;
SELECT 'Projects: SN --> SID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.SN FROM Projects R GROUP BY R.SN HAVING COUNT(DISTINCT R.SID) > 1) X;
SELECT 'Projects: SN --> PN' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.SN FROM Projects R GROUP BY R.SN HAVING COUNT(DISTINCT R.PN) > 1) X;
SELECT 'Projects: SN --> MID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.SN FROM Projects R GROUP BY R.SN HAVING COUNT(DISTINCT R.MID) > 1) X;
SELECT 'Projects: SN --> MN' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.SN FROM Projects R GROUP BY R.SN HAVING COUNT(DISTINCT R.MN) > 1) X;
SELECT 'Projects: PN --> ID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.PN FROM Projects R GROUP BY R.PN HAVING COUNT(DISTINCT R.ID) > 1) X;
SELECT 'Projects: PN --> PID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.PN FROM Projects R GROUP BY R.PN HAVING COUNT(DISTINCT R.PID) > 1) X;
SELECT 'Projects: PN --> SID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.PN FROM Projects R GROUP BY R.PN HAVING COUNT(DISTINCT R.SID) > 1) X;
SELECT 'Projects: PN --> SN' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.PN FROM Projects R GROUP BY R.PN HAVING COUNT(DISTINCT R.SN) > 1) X;
SELECT 'Projects: PN --> MID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.PN FROM Projects R GROUP BY R.PN HAVING COUNT(DISTINCT R.MID) > 1) X;
SELECT 'Projects: PN --> MN' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.PN FROM Projects R GROUP BY R.PN HAVING COUNT(DISTINCT R.MN) > 1) X;
SELECT 'Projects: MID --> ID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.MID FROM Projects R GROUP BY R.MID HAVING COUNT(DISTINCT R.ID) > 1) X;
SELECT 'Projects: MID --> PID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.MID FROM Projects R GROUP BY R.MID HAVING COUNT(DISTINCT R.PID) > 1) X;
SELECT 'Projects: MID --> SID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.MID FROM Projects R GROUP BY R.MID HAVING COUNT(DISTINCT R.SID) > 1) X;
SELECT 'Projects: MID --> SN' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.MID FROM Projects R GROUP BY R.MID HAVING COUNT(DISTINCT R.SN) > 1) X;
SELECT 'Projects: MID --> PN' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.MID FROM Projects R GROUP BY R.MID HAVING COUNT(DISTINCT R.PN) > 1) X;
SELECT 'Projects: MID --> MN' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.MID FROM Projects R GROUP BY R.MID HAVING COUNT(DISTINCT R.MN) > 1) X;
SELECT 'Projects: MN --> ID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.MN FROM Projects R GROUP BY R.MN HAVING COUNT(DISTINCT R.ID) > 1) X;
SELECT 'Projects: MN --> PID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.MN FROM Projects R GROUP BY R.MN HAVING COUNT(DISTINCT R.PID) > 1) X;
SELECT 'Projects: MN --> SID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.MN FROM Projects R GROUP BY R.MN HAVING COUNT(DISTINCT R.SID) > 1) X;
SELECT 'Projects: MN --> SN' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.MN FROM Projects R GROUP BY R.MN HAVING COUNT(DISTINCT R.SN) > 1) X;
SELECT 'Projects: MN --> PN' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.MN FROM Projects R GROUP BY R.MN HAVING COUNT(DISTINCT R.PN) > 1) X;
SELECT 'Projects: MN --> MID' AS FD, CASE WHEN COUNT(*)=0 THEN 'MAY HOLD' ELSE 'does not hold' END AS VALIDITY FROM (SELECT R.MN FROM Projects R GROUP BY R.MN HAVING COUNT(DISTINCT R.MID) > 1) X;
